services:
  voice-agent:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    volumes:
      - conversations:/app/runtime
      - indexes:/app/artifacts
    restart: unless-stopped

volumes:
  conversations:
  indexes:
